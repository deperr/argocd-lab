apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: app-02
data:
  default.conf: |
    server {
      listen 80;
      keepalive_timeout 0;
      location / {
        root /usr/share/nginx/html;
        index index.html;
      }
    }
